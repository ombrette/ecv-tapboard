ECV tapboard - Projet MEANstack
========================

Step to run locally:

* Clone this repo
* Run 'npm install'
* Run 'mongod --dbpath db/data' to launch the database
* Run 'npm start'
* Go in the "client" folder and run 'ng build -prod --output-path ../www'
* Launch the game on 'http://localhost:8080'
